---
export interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const classes = [
  'sg-sub-section',
  className
].filter(Boolean).join(' ');

---
<div class={classes}>
  <slot />
</div>

<style>
@layer sgb {
  .sg-sub-section {
    margin-bottom: 1rem;
  }
}
</style>